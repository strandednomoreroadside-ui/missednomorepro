# CLAUDE.md — Missed No More Pro OS

Multi-tenant SaaS: AI receptionist + field-service OS for local service businesses (1–15 employees). The operator is a **non-developer** — Claude codes everything; they handle accounts, keys, payments, and test calls. Explain things in plain English.

## The two source-of-truth documents

1. **BUILD_GUIDE.md** — the milestone roadmap (M0–M10). Always know which milestone we're on; never build ahead of it.
2. **docs/master-plan-v3.md** — the full product spec (schema §8, security §9, AI tool contracts §10, phases §11, pricing §6).

## Current state (update this section as milestones complete)

- ✅ M1 scaffold: Next.js 16 + TS strict + Tailwind v4 + shadcn-style components, brand theme, landing page, legal pages (drafts — finalize at M10), env validation
- ✅ M0 accounts: Vercel/Supabase/Stripe/Twilio/OpenAI all exist (user, June 2026). Remaining: domain (confirm), fill `.env.local`
- ✅ A2P 10DLC: **already approved** on the Twilio brand — no SMS-registration wait; attach number to the approved campaign at M6
- ✅ Voice engine: **decided — Retell (Path A)**, June 2026. Still build the §3.1 provider adapter so OpenAI Realtime direct stays swappable. `VOICE_PROVIDER=retell`, key in `RETELL_API_KEY`
- ✅ M1 complete: deployed on Vercel (git auto-deploy connected), domain `missednomorepro.com` live, all env vars in Vercel, Twilio number owned (+14406442423), `.env.local` filled
- ✅ M2 complete (June 2026): auth + orgs + RLS live in prod; migration applied via SQL-editor paste (table grants included — SQL editor didn't apply default privileges, explicit grants required); **leak test passed 8/8** (`scripts/leak-test.mjs` — rerun before beta)
- ✅ M3 complete (June 12 2026): Stripe test-mode billing live — 5 plans (monthly+annual), Checkout, Customer Portal (API-configured), signature-verified idempotent webhook, plan_limits/subscriptions/usage_events in prod, feature-gate + usage-limit helpers, billing page with usage, dashboard locked-features card. Operator verified: 4242 test subscription, portal, locked features. `/admin/billing-setup` = one-tap Stripe setup + live status checks (requires `ADMIN_EMAILS` in Vercel). Webhook signing secret lives in Vercel env (`STRIPE_WEBHOOK_SECRET`); retrievable anytime in Stripe dashboard → Webhooks → reveal
- ✅ M4 complete (June 12 2026, operator-verified): setup wizard at `/dashboard/setup` — 10 steps, saved progress (operator confirmed mid-wizard resume works), DB-enforced launch gate (trigger + `launch_business`/`approve_setup_section` RPCs; approval stamps not writable by clients), admin sees incomplete setups
- ✅ M5 complete (June 12 2026, operator-verified): CRM at `/dashboard/contacts` — contacts (tags, §8.3 consent fields, phone unique per tenant for M7 caller matching), leads, notes, tamper-proof timeline (trigger-written, clients read-only). **Leak test passed 18/18** — rerun before beta
- ✅ M6 complete (June 13 2026, operator-verified): phone_numbers/agents/calls/call_transcripts tables (members read-only; server writes); signature-validated Twilio voice webhook `/api/twilio/voice` (greeting + voicemail-to-log, known-caller matching to CRM, timeline event on call end) + `/status` + `/recording` callbacks; voicemail playback proxy `/api/recordings/[callId]`; `/dashboard/calls` + `/dashboard/settings`; admin number assignment; `scripts/twilio-setup.mjs` pointed +14406442423's webhooks at prod. Operator placed a real test call — greeting answered, call logged. **Leak test passed 21/21** (`scripts/leak-test.mjs`, now incl. M6 checks 15-16) — rerun before beta. Next: M7 builds the Retell integration on top of this phone foundation
- ✅ **M7 complete (June 13 2026, operator-verified)**: the AI receptionist. Built: §3.1 voice adapter (`src/lib/voice/*`, Retell impl on `retell-sdk` 5.36, OpenAI Path B stub, factory keyed on `VOICE_PROVIDER`); per-tenant prompt builder baking in §5.1 hard rules (`prompt.ts`, lazy re-sync via prompt hash stored on `agents`); 8-tool §10 router at `/api/voice/tools` — lookup_contact, create_contact, search_knowledge_base (wizard FAQs), check_service_area, notify_staff (**Twilio SMS** to staff — operator chose texts June 13 2026; `TWILIO_MESSAGING_SERVICE_SID` optional for A2P deliverability, falls back to the number), escalate_to_human, mark_spam, create_follow_up_task — **tenant resolved from the call row, never from LLM args**, zod-validated, audit + `tool_calls` logged; encrypted-raw + redacted-display transcripts (`crypto.ts`/`redact.ts`/`finalize.ts`, §9); disposition derived from AI actions; per-minute metering into `usage_events`; call-summary UI `/dashboard/calls/[id]`; Retell agent provisioning + `registerPhoneCall` + Twilio↔Retell media-stream bridge in `/api/twilio/voice` (M6 greeting is the graceful fallback when not live / AI off / error); secret-authed idempotent webhook `/api/voice/retell/webhook`. Migration `supabase/migrations/20260613120000_ai_receptionist.sql` (agents/calls extensions, `follow_up_tasks`, `tool_calls`). Secrets `INTERNAL_API_SECRET` + `TRANSCRIPT_ENCRYPTION_KEY` generated → in `.env.local`. **build + typecheck green. Operator ran the BUILD_GUIDE 10-call test — all 10 PASS (voice + data verified); the staff lead-alert text landed immediately.** Migration applied, `INTERNAL_API_SECRET`/`TRANSCRIPT_ENCRYPTION_KEY` in Vercel, "Stranded No More Roadside Assist" launched on +14406442423. **Critical learning — Retell custom-telephony bridge is "Dial to SIP URI": TwiML `<Dial><Sip>sip:{call_id}@sip.retellai.com</Sip></Dial>` after `registerPhoneCall`, NOT `<Connect><Stream wss://…>` (the media-stream form answers then drops silently — call_status stays `registered`).** Returning callers recognized in the first sentence via a per-call `{{opening_line}}` dynamic variable on `begin_message`. notify_staff/escalate text staff via `src/lib/twilio/sms.ts` (M7 chose SMS). Retell model `gpt-4.1` (tune in `retell.ts`). Read-only diagnostics: `scripts/m7-{diagnose,call-debug,call-inspect,verify}.mjs`. TODO before beta: extend `scripts/leak-test.mjs` for `follow_up_tasks` + `tool_calls`
- ✅ **M8 complete (June 14 2026, operator-verified)**: SMS system & compliance. Built: `messages` table (§8.2, encrypted + redacted bodies) + `sms_suppressions` (tenant-wide STOP list) + text-back settings on `sms_settings` (migration `20260614090000_sms_system.sql`); the **gated** outbound sender `src/lib/sms/outbound.ts` — `sendCustomerSms` (STOP always wins, consent required for non-transactional) + `sendStaffSms`, with M7 staff alerts now routed through it (so they're logged); inbound webhook `/api/twilio/sms` (STOP/START/HELP, suppression, signature-validated, idempotent on MessageSid); **missed-call text-back** `src/lib/sms/textback.ts` fired from the voice status callback (AI calls matched by `twilio_call_sid`; the status callback no longer clobbers Retell finalization for AI calls); `send_sms` voice tool (consent-gated, 9th §10 tool — prompt hash bumps so agents re-sync); message log UI `/dashboard/messages` + nav; SMS timeline events via trigger; editable text-back in Settings. Shared Twilio webhook parser moved to `src/lib/twilio/webhook.ts` (voice `shared.ts` re-exports). **build + typecheck green. Operator-verified June 14 2026** (`scripts/m8-verify.mjs`): missed-call text-back fired with the tenant template; STOP/START reached our webhook → suppression toggled + our acks sent; every message logged. Twilio configured: number SmsUrl + Messaging Service `MGefdfc0a6481dbd6bbc9e85d1ea328633` inbound → `/api/twilio/sms` (SID in `.env.local`; add to Vercel for best outbound A2P deliverability). **Known/accepted: HELP is answered by Twilio's built-in responder (intercepted before our webhook) — compliant; operator chose to keep it and revisit our branded HELP in v2 (would disable the Service's HELP keyword so it forwards to us). STOP/START DO forward to us.** TODO before beta: extend `scripts/leak-test.mjs` for `messages` + `sms_suppressions`
- ✅ **M9 complete (June 14 2026, operator-verified)**: calendar booking & jobs. Built: migration `20260615090000_calendar_jobs.sql` — `calendar_connections` (per-business Google OAuth; **tokens encrypted via `crypto.ts` AND token columns NOT granted to `authenticated` — only service role reads them**), `appointments` (**no-double-booking enforced in the DB: `exclude using gist (business_id =, tstzrange(starts_at,ends_at) &&) where status='confirmed'` + `btree_gist`**), `jobs` (members manage), `job_status_events` (trigger trail), `sms_settings.booking_confirmation_template`, appointment/job timeline triggers. Thin Google client `src/lib/google/*` (raw `fetch`, no `googleapis` SDK): `credentials.ts` parses base64 `GOOGLE_OAUTH_CREDENTIALS`, `oauth.ts` (consent/exchange/refresh/revoke, email from id_token), `calendar.ts` (freeBusy/insertEvent/deleteEvent), `connection.ts` (encrypted token storage + on-demand access-token refresh). DST-safe availability engine `src/lib/calendar/*` (`timezone.ts` Intl-based `zonedTimeToUtc` — verified across EDT/EST/CDT/Phoenix; `availability.ts` slots from business hours minus DB appts + Google busy). Two new §10 tools (router unchanged, tenant from call row): `check_calendar_availability` + `book_appointment` (re-validates hours + future + Google-free; **exclusion-constraint `23P01` = "slot just taken"**; books → Google event → job → `booked` disposition → confirmation SMS via `sendCustomerSms` kind `confirmation`, transactional, STOP still wins). **Booking self-enables when a calendar is connected**: `prompt.ts` swaps hard-rule #4 ("never book") for booking guardrails when `bookingEnabled`, hash bumps → lazy agent re-sync; voice route injects `{{current_date}}/{{current_day}}/{{current_time}}` (business-local). OAuth web flow: connect/disconnect server actions (CSRF state cookie) + `/api/google/callback`; Settings "Calendar booking" card + booking-confirmation editor; **Jobs page `/dashboard/jobs` + nav** (replaced the M9 placeholder). `scripts/m9-verify.mjs` (tz self-check + calendar/appt/job dump); leak-test extended → **checks 17-20** (appt/job/calendar isolation, appts not member-writable, **OAuth token columns unreadable by members**, job_status_events not forgeable). **build + typecheck green. Leak test passed 27/27** (`scripts/leak-test.mjs`, M9 checks 17-20 incl. **OAuth token columns return "permission denied" for members** while safe columns read fine). **Operator-verified June 14 2026**: migration applied; `GOOGLE_OAUTH_CREDENTIALS` (Web-app type — NOT the "installed"/Desktop type, which only registers `http://localhost` and fails) in `.env.local` + Vercel **Production** (needs a redeploy to take effect); connected Google (`strandednomorecle@gmail.com`); test call booked **Tue 7am → `confirmed` + `sync=synced` (real gcal event) + confirmation SMS sent + job created** (`scripts/m9-verify.mjs`). 3 AM refusal is enforced structurally (availability never emits out-of-hours slots + `isWithinBusinessHours` guard in `book_appointment`). **Watch item (not a blocker): the booking test call also fired two `escalate_to_human` staff alerts — review whether the agent over-escalates.** NOTE: booking is NOT plan-gated yet (§6.1 has it on Book+); defer to M10. Default appt length 60 min / 30-min slots / 60-min lead / 14-day horizon (constants in `availability.ts`). Google OAuth consent stays in Testing mode (≤100 users, no Google review) — expect the "unverified app" interstitial until M10
- ✅ **Pricing engine complete (June 14 2026, operator-verified — seeded, approved, quoting LIVE)**: deterministic quotes — master plan §10 `calculate_quote` / Phase 9, pulled forward at operator request after M9 (NOT plan-gated yet). **The AI speaks EXACT prices but every number is computed server-side by `calculate_quote` from owner-approved rules + Google Maps driving distance — never the LLM** (preserves §5.1 "never invent prices" + §14 0%-halluc gate). Migration `20260616090000_pricing_engine.sql`: `pricing_settings` (home-base address+geocode, `max_service_miles`, currency, `approved_at` gate), `pricing_zones` (distance-banded dispatch fee), `service_pricing` (flat OR tow=hook+per_mile; `variable_part` for "+cost of tire/battery/fuel"; availability window), `pricing_surcharges` (`auto_time` vs `conditional`); members manage. Pure engine `src/lib/pricing/engine.ts` (Total = zone dispatch + service fee + auto time-window surcharges; conditional surcharges surfaced not added; **tow = zone + $65 hook + $2.50/mi × tow-miles → 2 distance lookups**; out-of-area > max miles; availability windows) + `loader.ts` (`isQuotingEnabled` gate). Maps `src/lib/maps/client.ts` (Geocoding + Distance Matrix, raw fetch, `GOOGLE_MAPS_API_KEY`). 11th §10 tool `calculate_quote` (service + location [+ destination for tow]; returns itemized + a `say` line; tenant from call row). **Quoting self-enables on approval**: `prompt.ts` swaps rule 2 ("owner will text a quote") for "quote ONLY via calculate_quote, read back the exact total" when `quotingEnabled`; hash bumps → lazy re-sync. Review/approve UI `/dashboard/pricing` + nav (displays seeded rules; **Approve** geocodes base & stamps `approved_at`). Seed `scripts/seed-pricing.mjs` = Stranded No More's real sheet (base 6466 Haviland Dr Brook Park; Z1≤8/$65, Z2≤16/$75, Z3≤25/$85; jump $45/lockout $55/tire-w-spare $65/tire-no-spare $85+tire 9–4/battery $55+batt/fuel $45+fuel/tow $65 hook+$2.50mi; late-night +$20 auto 9p–5a; dangerous/weather/garage conditional; deposits SKIPPED per operator). `scripts/pricing-verify.mjs` math golden cases all pass. **build+typecheck green; `GOOGLE_MAPS_API_KEY` verified live (geocode + distance OK, base→downtown Cleveland 14.1mi=Zone2).** **Operator completed June 14 2026**: migration applied, key in Vercel, ran `seed-pricing.mjs` (base geocoded 41.39,-81.84), reviewed + approved in `/dashboard/pricing` → **quoting LIVE**; shipped as commit `784424b`. `scripts/pricing-verify.mjs` golden cases all pass. Service radius currently = `max_service_miles` (25); **NOTE: `check_service_area` is still the M7 zip/city-list version** until the planned radius rewrite (see "next" below). Remaining real-world check: a live on-call quote test
- 🟡 **Post-pricing UX batch (June 14 2026, built — pending deploy + operator test; build+typecheck green)** — three operator-requested items, none need a migration or new setup (all live on deploy): **(1) FAQ manager UI** `/dashboard/faqs` + nav — add/edit/toggle/delete FAQs post-setup (CRUD on the existing `faqs` table, which already feeds `search_knowledge_base`). **(2) Radius-based service area** — `check_service_area` rewritten to driving-distance-from-home-base ≤ radius (reuses `pricing_settings.base_lat/lng` + `max_service_miles`; one Distance Matrix lookup, geocodes the caller's city/zip), falling back to the legacy `service_areas` zip/city list when no base/maps. Radius is editable on `/dashboard/pricing` (`updateServiceRadius`). **Validated live: Lakewood 8mi + Strongsville 6mi = COVERED, Akron 38mi = out of area.** **(3) Call-ending fix (cost)** — `retell.ts` now adds Retell's built-in `end_call` tool + `end_call_after_silence_ms=15000`; `prompt.ts` intro pushes efficiency and the wrap-up says to call `end_call` and not linger. Takes effect on the next call after deploy (prompt-hash bump → lazy agent re-sync).
- 🟡 **Voice = "Grace" + Prices & Services manager (June 15 2026, built — build+typecheck green)**: **(1) AI voice** set to `11labs-Grace` — `DEFAULT_VOICE_ID` in `prompt.ts` AND `agents.voice_id` in DB both updated (the latter via service role; **necessary because `syncAgent` re-writes Retell's voice from `agents.voice_id` on every re-sync — changing it only in the Retell dashboard reverts on the next call**; see memory `ai-voice-is-grace`). **(2) Prices & Services manager** — `/dashboard/pricing` is now full CRUD (was review-only): add/delete/toggle services (flat or tow, `+cost of part`, availability window), add/delete zones, add/delete surcharges, set home-base address (`setHomeBase`), editable radius. Members manage (RLS); the quote engine reads only `active` rows. Approve button disabled until ≥1 zone + ≥1 active service. No migration (reuses pricing tables).
- 🟡 **Services-from-sheet fix + price update (June 15 2026, built + seeded live; build green)**: **(1) Bug fix** — the AI's spoken service list came only from the M4 wizard `services` table (2 services). `loadPromptInput` now UNIONS that with active `service_pricing` names (dedupe by name) so the AI knows all offered services; prompt-hash bumps → re-sync next call. **(2) Prices updated** (re-seeded live, approval preserved): zones $55/$65/$75, every service −$5 (Jump 40 / Lockout 50 / Tire-spare 60 / Tire-nospare 80 / Battery 50 / Fuel 40), tow $60 hook + $2.50/mi after **5 free miles** — new `free_miles` column on `service_pricing` (migration `20260617090000_tow_free_miles.sql`); engine subtracts free miles. **Gotcha: a bulk insert sends `null` for any key missing from a row, so the seed sets `free_miles` on every service row.** `pricing-verify.mjs` golden cases updated + all pass.
- 🟡 **Live human handoff / warm transfer (June 15 2026, built — build green; verify on a live call)**: the engineer-checklist "critical piece." `transfer_to_human` is a Retell **warm-transfer** tool (added in `retell.ts` when a transfer number exists) — it privately briefs the teammate (caller name + location + need/issue) THEN bridges, so the caller never re-explains. Destination = the business's first `notify_on_lead` staff contact (`agent-sync` loads it → `VoiceAgentConfig.transferNumber`, in the prompt hash so it re-syncs). `prompt.ts` escalation step (when `transferEnabled`) routes upset/distressed/complaint/"get me a person" to transfer, **falling back to `escalate_to_human`** (message + urgent alert) if it doesn't connect. Takes effect on the next call after deploy (hash bump → lazy re-sync). NOTE: warm transfer needs a human to actually answer the staff number; after-hours it falls back to the message path.
- 🟡 **Knowledge Hub — unify (June 15 2026, built — build green)**: `/dashboard/knowledge` overview ties together **Business basics** (industry/open-days/radius → links to Setup), **Services & pricing** (active service count + quoting on/off badge → `/dashboard/pricing`), and **FAQs** (active count → `/dashboard/faqs`). Nav consolidated: a single **Knowledge Hub** link replaced the separate Pricing + FAQs nav links (both pages still exist, reached via the hub).
- 🟡 **Knowledge Hub — doc upload & extract (June 15 2026, built — build+typecheck green; needs migration applied + operator test)**: the fast-follow is done. Migration `20260618090000_knowledge_documents.sql` — `knowledge_documents` (one row per upload; status uploaded→processing→extracted/failed) + `knowledge_suggestions` (approval queue, `kind` faq|service, structured `payload` jsonb) + a **private Supabase Storage bucket `knowledge-docs`** (no `storage.objects` policies → reachable only via the service-role admin client; files namespaced `{tenant_id}/{document_id}`). Members manage; explicit grants. Extraction `src/lib/knowledge/extract.ts` — **raw fetch to OpenAI `gpt-4.1-mini`, `response_format` json_schema (strict)**, accepts PDF/PNG/JPG/WEBP/TXT/CSV/MD ≤10MB (PDFs via chat `type:"file"` file_data, images via `image_url`, text inlined); model only transcribes **what's written, never computes a price** (preserves §5.1 + §14). Upload flow `src/app/dashboard/knowledge/upload/{page,actions}.tsx` (`maxDuration=60`): upload→store→extract→queue; **approving a suggestion inserts a real STRUCTURED row into `faqs`/`service_pricing`** (service approvals still need pricing re-approval in `/dashboard/pricing` to quote), plus reject/retry/delete. Hub "Upload documents" card now live (links to the page, shows an "N to review" badge). Leak test extended **checks 21-22** (B can't see/approve A's knowledge docs/suggestions) — rerun before beta (still also TODO: follow_up_tasks, tool_calls, messages, sms_suppressions). **No new env** (reuses OPENAI_API_KEY + Supabase). Covers FAQs + flat/tow services; zones/surcharges stay owner-entered on `/dashboard/pricing`. **Operator TODO: paste the migration into the Supabase SQL editor (creates the bucket too), redeploy, then test an upload.** **Update (June 16 2026): extraction now caps at 20 line items/doc (`MAX_LINE_ITEMS` in `extract.ts`, prompt + defensive trim, services prioritized) and the upload page has per-document "Approve all (N)" / "Reject all" (shared `applySuggestion` helper) — build+typecheck green.**
- 🟡 **Reminders + cancel/reschedule (roadmap #3) (June 16 2026, built — build+typecheck green; needs migration + CRON_SECRET + operator test)**: **(1) Reminders** — migration `20260619090000_reminders.sql` adds `sms_settings.{reminder_enabled default true, reminder_lead_hours default 24 (1–168), reminder_template}` + `appointments.reminder_sent_at` (idempotency stamp) + a partial index. **Daily Vercel Cron** `vercel.json` → `/api/cron/reminders` (auth: `Authorization: Bearer ${CRON_SECRET}`; allowed unauthenticated only outside prod). `src/lib/sms/reminders.ts#sendDueReminders` scans confirmed/future/un-reminded appts within an 8-day horizon, per-business gates on `reminder_enabled` + `starts_at ≤ now + lead_hours`, sends ONE text via `sendCustomerSms` (new `reminder` kind, transactional, STOP wins) and **stamps `reminder_sent_at` on send OR terminal skip** (suppressed/no-phone) so we never double-charge SMS; only transient Twilio errors retry next run. **Margin note:** one SMS/appt, daily cron = free Vercel tier; endpoint is frequency-agnostic so hourly on Pro just improves coverage (same-day bookings after the daily run may miss). Settings card (shown when calendar connected): toggle + lead-hours + editable template (`updateReminders`). **(2) Cancel/reschedule** — two new §10 voice tools `cancel_appointment` + `reschedule_appointment` (handlers + registry + TOOLS; tenant from call row, zod-validated, audit-logged). They resolve the caller's contact (ctx or by number), find upcoming confirmed appts, and `pickAppt` (explicit `start` within 5 min wins, else the single one, else `needs_selection` for the AI to disambiguate). Cancel → status `canceled` + delete Google event (best-effort) + job `canceled` + cancellation SMS. Reschedule → re-validate future/in-hours/Google-free, move appt (exclusion constraint `23P01` guards), delete+re-insert Google event, move job `scheduled_for`, confirmation SMS with new time. `prompt.ts` booking step now also covers cancel/reschedule (hash bumps → lazy agent re-sync). No new tenant tables → existing leak-test appt isolation (check 17) covers it. **Operator TODO: apply migration `20260619090000_reminders.sql`, set `CRON_SECRET` in Vercel (Production), redeploy; reminders fire on the daily cron. Test a cancel + reschedule on a live call.**
- 🧭 **Product vision + pricing re-architecture (operator, June 16 2026) — APPROVED PLAN, build phase-by-phase**: full plan at `C:\Users\Stran\.claude\plans\first-before-m10-we-snappy-deer.md`. Positioning shift to **"AI Receptionist + Smart CRM + AI Business Assistant"** (stay receptionist-attached, NOT a general CRM). **Pricing:** rename `answer/book/revenue/scale/agency` → **Starter $99 / Growth $199 / Professional $349 / Elite $599 / Enterprise (custom)**; **trim included minutes to 70%-safe levels (250/500/900/1500) + metered overage (~$0.20/min, $0.02/txt)** because voice (~$0.10–0.13/min: Retell+LLM+11labs+Twilio) is the only material COGS and the proposed allotments broke 70% margin on Pro/Elite. **Add-ons** as separate Stripe items + `tenant_addons` entitlements (`hasAddon`): Outbound Assistant +$49, Omnichannel Chat +$29, Business Assistant +$39, Growth Suite bundle +$100, Reputation Manager +$29, Call Intelligence +$19. **15-phase roadmap** (Part C of the plan): ✅Ph1 bulk-approve+line-cap (done June 16) → ✅Ph2 re-tier+gates (done June 16 — migration `20260620090000_plan_retier.sql`: renamed plans to Starter/Growth/Professional/Elite + custom Enterprise, minutes 250/500/900/1500, `overage_per_minute_cents`/`overage_per_sms_cents` cols, expanded `feature_flags_json` per tier; `plans.ts` rewritten + `effectivePlan` guards stale ids → 'none'; billing page Enterprise contact card; landing pricing updated; build+typecheck green, landing verified. **Operator TODO: apply the migration, then re-run `/admin/billing-setup` to create the new test Stripe prices**) → ✅Ph3 add-on billing + ✅Ph4 overage *display* (done June 16 — migration `20260621090000_addons.sql`: `tenant_addons` (members read, service-role write); add-on catalog `src/lib/billing/addons.ts` (Outbound $49 / Omnichannel $29 / Business Asst $39 / Growth Suite bundle $100 grants the 3 / Reputation $29 / Call Intel $19; `effectiveAddonKeys` expands bundle); add-ons sold as extra Stripe **subscription items** — `syncSubscription` now iterates ALL items (base plan + add-ons) and reconciles `tenant_addons`; `addAddon`/`removeAddon` server actions (subscriptionItems API + re-sync, prorated); `/admin/billing-setup` creates add-on prices + portal includes them; billing page add-ons grid (toggle, bundle shows members as "Included") + **overage projection** (uses `overage_per_*_cents`, shows "+N over ≈ $X"). Automated Stripe metered *charging* deferred to GA/Ph15 — pointless in test mode. build+typecheck green. **Operator TODO: apply `20260621090000_addons.sql`, re-run `/admin/billing-setup` to create the 6 add-on prices**) → ✅Ph5 lead pipeline + ✅Ph6 analytics dashboard (done June 16 — migration `20260622090000_lead_pipeline.sql` remaps `leads.status` to vision stages new_lead/quoted/scheduled/completed/repeat/follow_up/lost; `src/lib/crm/pipeline.ts` (`advanceLead` forward-only helper + STAGE_META); AI auto-advances: `calculate_quote`→quoted (+value), `book_appointment`→scheduled, `cancel_appointment`→follow_up, job marked completed→completed/repeat (new `jobs/actions.ts#updateJobStatus`, also tags contact "Customer"); kanban board `/dashboard/leads` + nav "Pipeline"; contact-page + manual stage controls updated; **dashboard rewritten into real analytics** (30-day KPIs: calls, answer rate=ai_handled%, booking rate, leads, pipeline value=Σ estimated_value of quoted+booked, missed-call texts) + pipeline snapshot; removed stale build-progress card. build+typecheck green; auth-gated so verified via build. **Operator TODO: apply `20260622090000_lead_pipeline.sql`**) → ✅Ph7 follow-up/outbound engine (done June 16 — migration `20260623090000_outbound_engine.sql`: `automations` (per-business per-kind config, members manage) + `outbound_queue` (deduped scheduled sends, service-role write/members read). Engine `src/lib/sms/outbound-engine.ts`: event-driven enqueue + daily `processOutboundQueue`; wired kinds **quote_followup** (enqueued in `calculate_quote`), **review_request** + **maintenance** (enqueued in `jobs/actions.ts#updateJobStatus` on completion); winback/campaigns/membership deferred (schema ready). **Margin/compliance:** every automation defaults OFF, consent required + STOP wins (`sendCustomerSms`, new `followup`/`review` kinds), deduped, per-run + per-business caps. New `src/lib/billing/entitlements.ts` (`getEntitlements`/`outboundEnabled` = plan `followup_campaigns` flag OR `outbound_assistant` add-on) gates sends at cron time. Daily Vercel Cron `/api/cron/outbound` (CRON_SECRET bearer; 2nd cron in `vercel.json` — Hobby allows 2). Settings UI `/dashboard/automations` + "Follow-ups" nav (toggle/delay/template per kind; shows add-on notice when not entitled). Leak test +checks 23-24 (automations/outbound_queue isolation; queue not member-writable). build+typecheck green; auth-gated UI verified via build. **Operator TODO: apply `20260623090000_outbound_engine.sql` (CRON_SECRET already needed for reminders covers it)**) → ✅Ph8 customer payments + LTV (done June 16, bundled Ph13's LTV slice — migration `20260624090000_payments.sql`: `payments` table (members manage; paid/refunded written by webhook). `src/lib/billing/payments.ts#createPaymentCheckout` = Stripe Checkout `mode:payment` with inline `price_data` (any amount, no pre-made Price; AI/staff never touch card data). `requestPayment`/`cancelPayment` actions in `contacts/actions.ts` (gated on `payment_requests` via entitlements): insert pending row → create Stripe link → text it (`sendCustomerSms` new `payment` kind, transactional, STOP wins). Stripe webhook `checkout.session.completed` mode=payment → marks the payment paid (idempotent, matched on `metadata.payment_id`). Contact page **Payments card** (request deposit/invoice/payment + history + **LTV total** = Σ paid). Dashboard **Collected (30d)** KPI. Leak test +check 25 (payments isolation). build+typecheck green; auth-gated UI verified via build. **NOTE: no AI `send_payment_link` voice tool yet** (deferred — needs deterministic deposit rules to preserve §5.1; staff-initiated only for now). **Operator TODO: apply `20260624090000_payments.sql` (no new env; reuses Stripe). Webhook already listens to `checkout.session.completed`.**) → Ph9 AI Business Assistant (links/deposits/invoices) → Ph9 AI Business Assistant → Ph10 omnichannel chat → Ph11 dispatch/calendar → Ph12 multi-user/location/membership → Ph13 MMS photo intake + LTV → Ph14a Reputation Manager + Ph14b Call Intelligence → Ph15 M10 hardening + Stripe live. ~60% of the vision is already shipped. Each phase returns for approval before building.
- 🔭 **Roadmap (operator, June 15 2026, superseded by the vision plan above)** — build order: ✅ live transfer → ✅ **Knowledge Hub** (unified overview + doc upload-and-extract; **pricing stays structured for calculate_quote, never free-text**) → ✅ reminders + cancellation/reschedule → M10 hardening (25 red-team, review cadence). Future asks: **CRM connectors** (a `CrmProvider` adapter à la the §3.1 voice adapter — Jobber/Housecall Pro/etc. + a generic webhook/Zapier escape hatch; our CRM stays default) and an **email channel** (AI triage/replies reusing the §10 tool brain). See memory `feature-roadmap`.
- ℹ️ DB migrations: applied by pasting the migration file into the Supabase SQL editor (clipboard flow). Supabase CLI not authenticated; `SUPABASE_DB_PASSWORD` in .env.local still empty

## Hard rules (from master plan §5.1, §9, §10 — never violate)

- `tenant_id` + RLS on **every** tenant-owned table; service-role key never in client code
- AI never: claims to be human, invents prices, takes card numbers by voice, books outside approved windows, texts opted-out contacts
- Every risky AI action goes through a §10 backend tool with server-side validation + audit log
- Webhooks (Stripe/Twilio): signature-validated + idempotent, always
- Stripe stays in **test mode** until M10
- Scope discipline: pricing engine, deposits, dispatch, invoicing, reviews, reporting, command center are **post-MVP** — do not build early

## Conventions

- App Router server components by default; `src/` layout; `@/*` imports
- Brand: dark-first only. Tokens in `src/app/globals.css` from `brand/missed_no_more_pro_brand_colors.txt` (night `#020817`, navy `#0A1B3D`, cyan `#00E5FF`, blue `#006BFF`, steel `#A7B0C0`). Fonts: Bricolage Grotesque (display) / Instrument Sans (body) / IBM Plex Mono (numbers, labels)
- Env vars: add to `src/lib/env.ts` schema + BUILD_GUIDE template together; everything optional until its milestone needs it
- Verify before done: `npm run build` + `npm run typecheck` pass; commit per milestone step
- Budget: ~$50–70/mo until revenue — prefer free tiers, flag anything that costs money **before** doing it
